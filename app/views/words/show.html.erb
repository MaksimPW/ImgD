<p id="notice"><%= notice %></p>
<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th class="col-md-2"><%= t('words.img-association') %></th>
      <th class="col-md-4"><%= t('words.word') %></th>
      <th class="col-md-4"><%= t('words.desc') %></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= image_tag @word.img.url(:medium) %></td>
        <td><%= @word.word %></td>
        <td class='Description'><%= @word.desc %></td>
      </tr>
  </tbody>
</table>

<%= link_to t('control.edit'), edit_word_path(@word) %> |
<%= link_to t('control.back'), words_path %>

